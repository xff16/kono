services:
  bravka:
    build:
      context: .
      dockerfile: build/Dockerfile
      platforms:
        - linux/amd64
    ports:
      - '8090:8090'
      - '8095:8095'
    volumes:
      - ./bravka.yaml:/app/bravka.yaml
    environment:
      BRAVKA_CONFIG: ./bravka.yaml